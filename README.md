# cuda_batch_image_pipeline_new
Batch image-processing project using CUDA. The program generates a large set of synthetic grayscale images and processes them in a single execution. Each image passes through three stages: Gaussian blur, Sobel edge detection, and binary thresholding.
